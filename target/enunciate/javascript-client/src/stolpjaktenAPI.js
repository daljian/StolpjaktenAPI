/**
 * 
 *
 * Generated by <a href="http://enunciate.webcohesion.com">Enunciate</a>.
 */
 
/**
 * UMD load definition that supports AMD, CommonJS and browser globals.
 * https://github.com/umdjs/umd/blob/992cc0d071b7ebcd7feaad2a5349bdbeab09a0fe/commonjsStrict.js
 */
(function (root, factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD. Register as an anonymous module.
    define(['exports'], factory);
  } else if (typeof exports === 'object') {
    // CommonJS
    factory(exports);
  } else {
    // Browser globals
    factory(root.javascriptClient = {});
  }
}(this, function (exports) {
 
 'use strict';
 

/**
 * (no documentation provided)
 */
ensureNamespace('Se.Stolpjakten.Api.Rest.Type');
exports.Se.Stolpjakten.Api.Rest.Type.Configuration = class {

    /**
     * Constructs a Configuration from a JSON object
     *
     * @param Object o JSON object.
     */
    constructor(o) {
        var self = this;
        if (typeof o['key'] !== 'undefined') {
            this.key = o['key'];
        }
        if (typeof o['value'] !== 'undefined') {
            this.value = o['value'];
        }
    }

    /**
     * Configuration key.
     *
     * @return string
     */
    getKey() {
        return this.key;
    }

    /**
     * Configuration key.
     *
     * @param string key
     */
    setKey(key) {
        this.key = key;
    }
    /**
     * Configuration value.
     *
     * @return string
     */
    getValue() {
        return this.value;
    }

    /**
     * Configuration value.
     *
     * @param string value
     */
    setValue(value) {
        this.value = value;
    }

    /**
     * Returns a JSON object for this Configuration
     *
     * @return object
     */
    toJSON() {
        var json = {};
        var self = this;
        if (typeof this.key !== 'undefined') {
            json['key'] = this.key;
        }
        if (typeof this.value !== 'undefined') {
            json['value'] = this.value;
        }
        return json;
    }

}

/**
 * (no documentation provided)
 */
ensureNamespace('Se.Stolpjakten.Api.Rest.Type');
exports.Se.Stolpjakten.Api.Rest.Type.Token = class {

    /**
     * Constructs a Token from a JSON object
     *
     * @param Object o JSON object.
     */
    constructor(o) {
        var self = this;
        if (typeof o['accessToken'] !== 'undefined') {
            this.accessToken = o['accessToken'];
        }
        if (typeof o['type'] !== 'undefined') {
            this.type = o['type'];
        }
        if (typeof o['expire'] !== 'undefined') {
            this.expire = o['expire'];
        }
    }

    /**
     * The Bearer access token to be used for token protected endpoints.
       * &lt;br&gt;&lt;br&gt;
       * Sample value:
       * &lt;br&gt;
       * 5eb19a75-c1d2-4d57-b1ec-4c61ff3c3cf2
     *
     * @return string
     */
    getAccessToken() {
        return this.accessToken;
    }

    /**
     * The Bearer access token to be used for token protected endpoints.
       * &lt;br&gt;&lt;br&gt;
       * Sample value:
       * &lt;br&gt;
       * 5eb19a75-c1d2-4d57-b1ec-4c61ff3c3cf2
     *
     * @param string accessToken
     */
    setAccessToken(accessToken) {
        this.accessToken = accessToken;
    }
    /**
     * Type of access token, currently only Bearer tokens are supported.
       * &lt;br&gt;&lt;br&gt;
       * Sample value:
       * &lt;br&gt;
       * Bearer
     *
     * @return string
     */
    getType() {
        return this.type;
    }

    /**
     * Type of access token, currently only Bearer tokens are supported.
       * &lt;br&gt;&lt;br&gt;
       * Sample value:
       * &lt;br&gt;
       * Bearer
     *
     * @param string type
     */
    setType(type) {
        this.type = type;
    }
    /**
     * The seconds left of the token life time.
       * 
       * &lt;br&gt;&lt;br&gt;
       * Sample value:
       * &lt;br&gt; 3600
     *
     * @return integer
     */
    getExpire() {
        return this.expire;
    }

    /**
     * The seconds left of the token life time.
       * 
       * &lt;br&gt;&lt;br&gt;
       * Sample value:
       * &lt;br&gt; 3600
     *
     * @param integer expire
     */
    setExpire(expire) {
        this.expire = expire;
    }

    /**
     * Returns a JSON object for this Token
     *
     * @return object
     */
    toJSON() {
        var json = {};
        var self = this;
        if (typeof this.accessToken !== 'undefined') {
            json['accessToken'] = this.accessToken;
        }
        if (typeof this.type !== 'undefined') {
            json['type'] = this.type;
        }
        if (typeof this.expire !== 'undefined') {
            json['expire'] = this.expire;
        }
        return json;
    }

}

/**
 * (no documentation provided)
 */
ensureNamespace('Se.Stolpjakten.Api.Rest.Type');
exports.Se.Stolpjakten.Api.Rest.Type.User = class {

    /**
     * Constructs a User from a JSON object
     *
     * @param Object o JSON object.
     */
    constructor(o) {
        var self = this;
        if (typeof o['email'] !== 'undefined') {
            this.email = o['email'];
        }
        if (typeof o['userName'] !== 'undefined') {
            this.userName = o['userName'];
        }
        if (typeof o['password'] !== 'undefined') {
            this.password = o['password'];
        }
    }

    /**
     * Email address for this user.
       * &lt;br&gt;
       * Required: no
     *
     * @return string
     */
    getEmail() {
        return this.email;
    }

    /**
     * Email address for this user.
       * &lt;br&gt;
       * Required: no
     *
     * @param string email
     */
    setEmail(email) {
        this.email = email;
    }
    /**
     * A globally unique username that identifies a user.
       * &lt;br&gt;
       * Required: yes
     *
     * @return string
     */
    getUserName() {
        return this.userName;
    }

    /**
     * A globally unique username that identifies a user.
       * &lt;br&gt;
       * Required: yes
     *
     * @param string userName
     */
    setUserName(userName) {
        this.userName = userName;
    }
    /**
     * Password for this user.
       * &lt;br&gt;
       * Only available for add (POST) and update (PUT) requests.
       * &lt;br&gt;
       * Stored in database using salted one-way hash algorithm with.
       * &lt;br&gt;
       * Required: yes
     *
     * @return string
     */
    getPassword() {
        return this.password;
    }

    /**
     * Password for this user.
       * &lt;br&gt;
       * Only available for add (POST) and update (PUT) requests.
       * &lt;br&gt;
       * Stored in database using salted one-way hash algorithm with.
       * &lt;br&gt;
       * Required: yes
     *
     * @param string password
     */
    setPassword(password) {
        this.password = password;
    }

    /**
     * Returns a JSON object for this User
     *
     * @return object
     */
    toJSON() {
        var json = {};
        var self = this;
        if (typeof this.email !== 'undefined') {
            json['email'] = this.email;
        }
        if (typeof this.userName !== 'undefined') {
            json['userName'] = this.userName;
        }
        if (typeof this.password !== 'undefined') {
            json['password'] = this.password;
        }
        return json;
    }

}

/**
 * (no documentation provided)
 */
ensureNamespace('Se.Stolpjakten.Api.Rest.Error');
exports.Se.Stolpjakten.Api.Rest.Error.BaseError = class {

    /**
     * Constructs a BaseError from a JSON object
     *
     * @param Object o JSON object.
     */
    constructor(o) {
        var self = this;
        if (typeof o['description'] !== 'undefined') {
            this.description = o['description'];
        }
    }

    /**
     * Human readable description for this error.
     *
     * @return string
     */
    getDescription() {
        return this.description;
    }

    /**
     * Human readable description for this error.
     *
     * @param string description
     */
    setDescription(description) {
        this.description = description;
    }

    /**
     * Returns a JSON object for this BaseError
     *
     * @return object
     */
    toJSON() {
        var json = {};
        var self = this;
        if (typeof this.description !== 'undefined') {
            json['description'] = this.description;
        }
        return json;
    }

}

/**
 * (no documentation provided)
 */
ensureNamespace('Se.Stolpjakten.Api.Db.Type');
exports.Se.Stolpjakten.Api.Db.Type.Users = class {

    /**
     * Constructs a Users from a JSON object
     *
     * @param Object o JSON object.
     */
    constructor(o) {
        var self = this;
        if (typeof o['userName'] !== 'undefined') {
            this.userName = o['userName'];
        }
        if (typeof o['email'] !== 'undefined') {
            this.email = o['email'];
        }
        if (typeof o['password'] !== 'undefined') {
            this.password = o['password'];
        }
    }

    /**
     * A globally unique username that identifies a user.
       * &lt;br&gt;
       * Required: yes
     *
     * @return string
     */
    getUserName() {
        return this.userName;
    }

    /**
     * A globally unique username that identifies a user.
       * &lt;br&gt;
       * Required: yes
     *
     * @param string userName
     */
    setUserName(userName) {
        this.userName = userName;
    }
    /**
     * Email address for this user.
       * &lt;br&gt;
       * Required: no
     *
     * @return string
     */
    getEmail() {
        return this.email;
    }

    /**
     * Email address for this user.
       * &lt;br&gt;
       * Required: no
     *
     * @param string email
     */
    setEmail(email) {
        this.email = email;
    }
    /**
     * Password for this user.
       * &lt;br&gt;
       * Only available for add (POST) and update (PUT) requests.
       * &lt;br&gt;
       * Stored in database using salted one-way hash algorithm with.
       * &lt;br&gt;
       * Required: yes
     *
     * @return string
     */
    getPassword() {
        return this.password;
    }

    /**
     * Password for this user.
       * &lt;br&gt;
       * Only available for add (POST) and update (PUT) requests.
       * &lt;br&gt;
       * Stored in database using salted one-way hash algorithm with.
       * &lt;br&gt;
       * Required: yes
     *
     * @param string password
     */
    setPassword(password) {
        this.password = password;
    }

    /**
     * Returns a JSON object for this Users
     *
     * @return object
     */
    toJSON() {
        var json = {};
        var self = this;
        if (typeof this.userName !== 'undefined') {
            json['userName'] = this.userName;
        }
        if (typeof this.email !== 'undefined') {
            json['email'] = this.email;
        }
        if (typeof this.password !== 'undefined') {
            json['password'] = this.password;
        }
        return json;
    }

}

/**
 * (no documentation provided)
 */
ensureNamespace('Se.Stolpjakten.Api.Rest.Error');
exports.Se.Stolpjakten.Api.Rest.Error.ErrorCode = {

    /**
     *  JSON is not syntactically  correct.
    *  &lt;br&gt;
    *  Please make sure to use JSON lib to create your payload.
     */
    JSON_SYNTAX: "JSON_SYNTAX", 

    /**
     *  JSON is not semantically correct.
    *  &lt;br&gt;
    *  Please consult API documentation to assert you follow the correct format.
     */
    JSON_SEMATIC: "JSON_SEMATIC", 

    /**
     *  JSON field &quot;userName&quot; is not correct.
    *  &lt;br&gt;
    *  Please consult Configuration setting: USERNAME_PATTERN 
     */
    JSON_FIELD_USERNAME: "JSON_FIELD_USERNAME", 

    /**
     *  JSON field &quot;email&quot; is not correct.
    *  &lt;br&gt;
    *  Email address must pass validation by javax.mail.internet.InternetAddress
     */
    JSON_FIELD_EMAIL: "JSON_FIELD_EMAIL", 

    /**
     *  JSON field &quot;password&quot; is not correct.
    *  &lt;br&gt;
    *  Please consult Configuration setting: PASSWORD_PATTERN 
     */
    JSON_FIELD_PASSWORD: "JSON_FIELD_PASSWORD", 

    /**
     *  Can not create resource, because it already exists.
    *  &lt;br&gt;
    *  POST another resource or use PUT to edit existing resource.
     */
    RESOURCE_EXISTS: "RESOURCE_EXISTS", 

    /**
     *  Requested resource was not found.
    *  &lt;br&gt;
    *  Could not find requested resource, maybe you want to POST to create it?
     */
    RESOURCE_NOT_EXISTS: "RESOURCE_NOT_EXISTS"
};

/**
 * (no documentation provided)
 */
ensureNamespace('Se.Stolpjakten.Api.Rest.Error');
exports.Se.Stolpjakten.Api.Rest.Error.InternalServerError = class extends exports.Se.Stolpjakten.Api.Rest.Error.BaseError {

    /**
     * Constructs a InternalServerError from a JSON object
     *
     * @param Object o JSON object.
     */
    constructor(o) {
        super(o);
        var self = this;
        if (typeof o['identifier'] !== 'undefined') {
            this.identifier = o['identifier'];
        }
    }

    /**
     * 
     *
     * @return string
     */
    getIdentifier() {
        return this.identifier;
    }

    /**
     * 
     *
     * @param string identifier
     */
    setIdentifier(identifier) {
        this.identifier = identifier;
    }

    /**
     * Returns a JSON object for this InternalServerError
     *
     * @return object
     */
    toJSON() {
        var json = super.toJSON();
        var self = this;
        if (typeof this.identifier !== 'undefined') {
            json['identifier'] = this.identifier;
        }
        return json;
    }

}

/**
 * (no documentation provided)
 */
ensureNamespace('Se.Stolpjakten.Api.Rest.Error');
exports.Se.Stolpjakten.Api.Rest.Error.NotFound = class extends exports.Se.Stolpjakten.Api.Rest.Error.BaseError {

    /**
     * Constructs a NotFound from a JSON object
     *
     * @param Object o JSON object.
     */
    constructor(o) {
        super(o);
        var self = this;
    }


    /**
     * Returns a JSON object for this NotFound
     *
     * @return object
     */
    toJSON() {
        var json = super.toJSON();
        var self = this;
        return json;
    }

}

/**
 * (no documentation provided)
 */
ensureNamespace('Se.Stolpjakten.Api.Rest.Error');
exports.Se.Stolpjakten.Api.Rest.Error.Unauthorized = class extends exports.Se.Stolpjakten.Api.Rest.Error.BaseError {

    /**
     * Constructs a Unauthorized from a JSON object
     *
     * @param Object o JSON object.
     */
    constructor(o) {
        super(o);
        var self = this;
    }


    /**
     * Returns a JSON object for this Unauthorized
     *
     * @return object
     */
    toJSON() {
        var json = super.toJSON();
        var self = this;
        return json;
    }

}

/**
 * (no documentation provided)
 */
ensureNamespace('Se.Stolpjakten.Api.Rest.Error');
exports.Se.Stolpjakten.Api.Rest.Error.BadRequest = class extends exports.Se.Stolpjakten.Api.Rest.Error.BaseError {

    /**
     * Constructs a BadRequest from a JSON object
     *
     * @param Object o JSON object.
     */
    constructor(o) {
        super(o);
        var self = this;
        if (typeof o['errorCode'] !== 'undefined') {
            this.errorCode = o['errorCode'];
        }
    }

    /**
     * The Error code.
     *
     * @return string
     */
    getErrorCode() {
        return this.errorCode;
    }

    /**
     * The Error code.
     *
     * @param string errorCode
     */
    setErrorCode(errorCode) {
        this.errorCode = errorCode;
    }

    /**
     * Returns a JSON object for this BadRequest
     *
     * @return object
     */
    toJSON() {
        var json = super.toJSON();
        var self = this;
        if (typeof this.errorCode !== 'undefined') {
            json['errorCode'] = this.errorCode;
        }
        return json;
    }

}

/**
 * (no documentation provided)
 */
ensureNamespace('Se.Stolpjakten.Api.Rest.Error');
exports.Se.Stolpjakten.Api.Rest.Error.Forbidden = class extends exports.Se.Stolpjakten.Api.Rest.Error.BaseError {

    /**
     * Constructs a Forbidden from a JSON object
     *
     * @param Object o JSON object.
     */
    constructor(o) {
        super(o);
        var self = this;
    }


    /**
     * Returns a JSON object for this Forbidden
     *
     * @return object
     */
    toJSON() {
        var json = super.toJSON();
        var self = this;
        return json;
    }

}
  
  /**
   * Ensures that a namespace exists.
   */
  function ensureNamespace(namespace){
    var current = exports;
    namespace.split('.').forEach(function(name){
      if(typeof current[name] === 'undefined'){
        current[name] = {};
      }
      current = current[name];
    });
  }

}));

